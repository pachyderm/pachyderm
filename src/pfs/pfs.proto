syntax = "proto3";

package pfs;

message GetFileRequest {
  string repository = 1;
  string commit_id = 2;
  string path = 3;
}

message StreamingBytes {
  bytes payload = 1;
}

service Api {
  rpc GetFile(GetFileRequest) returns (stream StreamingBytes) {}
}
