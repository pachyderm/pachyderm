// Package constants contains constants shared among packages.
package constants

const (
	// ContextTokenKey is the key of the auth token in an
	// authenticated context
	ContextTokenKey = "authn-token"

	// JSONSchemaKey is the key in JSON that names the schema of the document.
	JSONSchemaKey = "$schema"
)
