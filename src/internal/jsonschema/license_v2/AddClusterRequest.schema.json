{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$ref": "#/definitions/AddClusterRequest",
    "definitions": {
        "AddClusterRequest": {
            "properties": {
                "id": {
                    "type": "string",
                    "description": "id is the unique, immutable identifier for this cluster"
                },
                "address": {
                    "type": "string",
                    "description": "address is the public GPRC address where the cluster can be reached"
                },
                "secret": {
                    "type": "string",
                    "description": "If set, secret specifies the shared secret this cluster will use to authenticate to the license server. Otherwise a secret will be generated and returned in the response."
                },
                "userAddress": {
                    "type": "string",
                    "description": "The pachd address for users to connect to."
                },
                "clusterDeploymentId": {
                    "type": "string",
                    "description": "The deployment ID value generated by each Cluster"
                },
                "enterpriseServer": {
                    "type": "boolean",
                    "description": "This field indicates whether the address points to an enterprise server"
                }
            },
            "additionalProperties": false,
            "type": "object",
            "title": "Add Cluster Request"
        }
    }
}