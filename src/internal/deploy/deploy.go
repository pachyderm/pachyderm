package deploy // import "github.com/pachyderm/pachyderm/src/internal/deploy"

// DevVersionTag is used to specify and detect that we're using a non-released version of a pachd image
const DevVersionTag = "local"
