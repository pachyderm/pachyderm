pps:
  kind: node
  name: "2"
service: ubuntu
input:
  host:
    /tmp/pps-server-test/1-out: /in
output:
  host:
    /tmp/pps-server-test/2-out: /out
run:
  - for file in /in/*; do cp $file /out/$(basename $file).copy; done
