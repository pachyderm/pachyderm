syntax = "proto3";

import "pps/pps.proto";

package run;

message AddedPipelineRun {
  string pipeline_run_id = 1;
  pps.PipelineSource pipeline_source = 2;
  pps.Pipeline pipeline = 3;
}

message PipelineRunError {
  string pipeline_run_id = 1;
  string node = 2;
  string error = 3;
}
