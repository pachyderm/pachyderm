pps:
  kind: node
  name: bar-node
service: bar
input:
  /in: pfs://bar-repository
output:
  # outputs must be globally unique across all nodes
  /out: pfs://baz-out-repository
run:
  - for file in /in/*; do cp $file /out; done
