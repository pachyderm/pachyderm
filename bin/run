#!/bin/sh

set -Ee

DIR="$(cd "$(dirname "${0}")/.." && pwd)"
cd "${DIR}"

"${DIR}/bin/setup"
"${DIR}/bin/build"

if [ -z "${PACHYDERM_IMAGE}" ]; then
  PACHYDERM_IMAGE="pachyderm"
fi
docker-compose run --service-ports ${PACHYDERM_IMAGE} $@
