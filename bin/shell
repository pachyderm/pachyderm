#!/bin/sh

set -Ee

DIR="$(cd "$(dirname "${0}")" && pwd)"
cd "${DIR}"

. "${DIR}/lib/lib.sh"
. "${DIR}/../etc/env/pfs.env"

export PFS_DOCKER_OPTS="-it"
export PFS_IMAGE="${PFS_DEV_IMAGE}"
${DIR}/run /bin/bash
