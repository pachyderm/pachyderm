#!/bin/sh

set -Ee

DIR="$(cd "$(dirname "${0}")/.." && pwd)"
cd "${DIR}"

. "${DIR}/etc/env/pfs.env"

if [ -z "${PFS_BINARY}" ]; then
  echo "Usage: PFS_BINARY=name run-binary" >&2
  exit 1
fi

export PFS_DOCKER_OPTS="-d"
"${DIR}/bin/docker-compile"
"${DIR}/bin/run"
