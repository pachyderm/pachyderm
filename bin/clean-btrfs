#!/bin/sh

set -E

DIR="$(cd "$(dirname "${0}")" && pwd)"
cd "${DIR}"

. "${DIR}/lib/lib.sh"
. "${DIR}/../etc/env/pfs.env"

umount "${PFS_HOST_VOLUME}"
rm -rf "${PFS_HOST_VOLUME}"
rm "${PFS_DATA_IMG}"
exit 0
