#!/bin/sh

set -E

DIR="$(cd "$(dirname "${0}")" && pwd)"
cd "${DIR}"

. "${DIR}/../etc/env/pfs.env"

sudo umount "${PFS_HOST_VOLUME}"
sudo rm -rf "${PFS_HOST_VOLUME}"
sudo rm "${PFS_DATA_IMG}"
exit 0
