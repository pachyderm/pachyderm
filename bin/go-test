#!/bin/sh

set -Ee

DIR="$(cd "$(dirname "${0}")" && pwd)"
cd "${DIR}"

. "${DIR}/../etc/env/env.env"

if [ -z "${GOMAXPROCS}" ]; then
	GOMAXPROCS="${PACHYDERM_DEFAULT_GOMAXPROCS}"
fi

${DIR}/run go test -parallel "${GOMAXPROCS}" -timeout 20m $@
