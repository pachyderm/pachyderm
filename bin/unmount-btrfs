#!/bin/sh

set -E

DIR="$(cd "$(dirname "${0}")" && pwd)"
cd "${DIR}"

. "${DIR}/../etc/env/env.env"

sudo umount "${PFS_HOST_VOLUME}" > /dev/null 2> /dev/null
sudo rm -rf "${PFS_HOST_VOLUME}" > /dev/null 2> /dev/null
sudo rm "${PFS_DATA_IMG}" > /dev/null 2> /dev/null
exit 0
