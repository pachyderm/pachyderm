apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-notebooks
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-storage
  volumeName: local-volume
