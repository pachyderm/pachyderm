{
  "pipeline": {
    "name": "GoT_train"
  },
  "transform": {
    "image": "tensor_flow_rnn_got",
    "cmd": [ "sh" ],
    "stdin": [
      "cd /code && python ptb_word_lm.py --data_path=/data --model=test --model_path_prefix=/data > /pfs/out/model-results",
      "cp /data/ptb.ckpt* /pfs/out",
      "cp /data/*.json /pfs/out"
    ]
  },
  "parallelism": "1",
  "inputs": [
    {
      "repo": {
        "name": "GoT_scripts"
      },
      "method": "global"
    }
  ]
}
{
  "pipeline": {
    "name": "GoT_generate"
  },
  "transform": {
    "image": "tensor_flow_rnn_got",
    "cmd": [ "sh" ],
    "stdin": [
      "cp /pfs/GoT_train/ptb.ckpt* /data",
      "cp /pfs/GoT_train/*.json /data",
      "cd /code && python ptb_word_lm.py --model=test --generate=true --model_path_prefix=/data > /pfs/out/sentence" 
    ]
  },
  "parallelism": "1",
  "inputs": [
    {
      "repo": {
        "name": "GoT_train"
      },
      "method": "global"
    }
  ]
}
