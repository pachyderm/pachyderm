{{ if .Values.pachd.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  # TODO if and .Values.pachd.storage.serviceAccountName eq .Values.pachd.storage.backend "GOOGLE" 
  #annotations:
  #  iam.gke.io/gcp-service-account: "{{ .Values.pachd.storage.serviceAccountName  }}"
  # end
  labels:
    app: "" #TODO
    suite: pachyderm
  name: {{ .Values.pachd.serviceAccount.name }}
  namespace: {{ .Release.Namespace }}
{{ end }}