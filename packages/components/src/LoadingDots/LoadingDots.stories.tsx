import React from 'react';

import {LoadingDots} from '../LoadingDots';

/* eslint-disable-next-line import/no-anonymous-default-export */
export default {title: 'Loading Dots'};

export const Default = () => {
  return <LoadingDots />;
};
