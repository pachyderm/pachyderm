#!/bin/bash
# these can be set by folks running the examples/Makefile.testing to use a different binaries than in the path
ECHO=${ECHO:-echo}
JQ=${JQ:-jq}
KUBECTL=${KUBECTL:-kubectl}
PACHCTL=${PACHCTL:-pachctl}
RM=${RM:-rm}
SLEEP=${SLEEP:-sleep}
TAR=${TAR:-tar}
WC=${WC:-WC}
WGET=${WGET:-wget}

