apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    name: router
  name: router
spec:
  replicas: 1
  selector:
    name: router
  template:
    metadata:
      labels:
        name: router
    spec:
      containers:
        - name: router
          image: jdoliner/pfs
          command: [ "/go/src/github.com/pachyderm/pachyderm/etc/bin/launch-wrapper", "/go/bin/router", "8" ]
          ports:
          - containerPort: 80
            name: port
      restartPolicy: Always
